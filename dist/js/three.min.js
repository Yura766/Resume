import*as THREE from"https://cdn.skypack.dev/three@0.129.0/build/three.module.js";import{OrbitControls}from"https://cdn.skypack.dev/three@0.129.0/examples/jsm/controls/OrbitControls.js";import{GLTFLoader}from"https://cdn.skypack.dev/three@0.129.0/examples/jsm/loaders/GLTFLoader.js";const scene=new THREE.Scene,camera=new THREE.PerspectiveCamera(45,window.innerWidth/window.innerHeight,.1,1e3);camera.position.z=10;const renderer=new THREE.WebGLRenderer({alpha:!0,antialias:!0});renderer.setSize(window.innerWidth,window.innerHeight),document.body.appendChild(renderer.domElement);const container=document.getElementById("table-3D-obj");container.appendChild(renderer.domElement);const aLight=new THREE.AmbientLight(4210752,1.2);scene.add(aLight);const pLight=new THREE.PointLight(16777215,1.2);pLight.position.set(0,-3,7),scene.add(pLight);const geometry=new THREE.BoxGeometry,material=new THREE.MeshPhongMaterial({color:65280}),cube=new THREE.Mesh(geometry,material);scene.add(cube);const sphereGeometry=new THREE.SphereGeometry(1,32,32),sphereMaterial=new THREE.MeshPhongMaterial({color:16711680}),sphere=new THREE.Mesh(sphereGeometry,sphereMaterial);sphere.position.x=-3,scene.add(sphere);const planeGeometry=new THREE.PlaneGeometry(10,10,10,10),planeMaterial=new THREE.MeshPhongMaterial({color:255,side:THREE.DoubleSide}),plane=new THREE.Mesh(planeGeometry,planeMaterial);plane.rotation.x=-Math.PI/2,scene.add(plane);const controls=new OrbitControls(camera,renderer.domElement);let loader=new GLTFLoader;function animate(){requestAnimationFrame(animate),cube.rotation.x+=.01,cube.rotation.y+=.01,renderer.render(scene,camera)}loader.load("/models/table.gltf",(function(e){const n=e.scene;scene.add(n)})),animate();